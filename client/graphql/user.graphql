mutation Login($input: LoginInput! ) {
    login(input: $input)
}


mutation CreateUser($input: UserInput!) {
  createUser(input: $input) {
    id
    email
    firstName
    lastName
    role
  }
}


query GetUsers {
    getUsers {
        id
        email
        firstName
        role
    }
}